# truSDX-AI Driver Requirements
# Install with: pip3 install --user -r requirements.txt

pyaudio>=0.2.11
pyserial>=3.4

# System packages (install via apt):
# sudo apt install python3 python3-pip portaudio19-dev pulseaudio-utils

